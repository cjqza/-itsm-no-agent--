# 公司桌面IT服务台 — 优化方案 v2.0（工程质量与缺陷修复专版）

> 产品经理：Claude
> 日期：2026-07-23
> 版本：v2.0
> 定位：本版**不新增业务功能**，聚焦「已确认的 Bug + 工程质量 + 可维护性」，目标是把系统从"能演示"推进到"稳定可交付"。

---

## 一、为什么要做这一版（Context）

`产品优化计划书.md`（v1.0，2026-07-22）里列的 P0/P1 大多**已经实现**——文件上传、工单转派、催办/取消、用户管理、限流、消息已读、快捷回复模板、路由守卫、404 兜底等，对照 `FEATURES.md` 均已落地。因此 v1.0 的"补功能"清单已基本过时。

但通过对当前代码的逐文件核查，发现遗留下来的是另一类问题：**一批有明确代码证据的缺陷，和一层长期欠账的工程质量**。它们不影响 Demo 演示，却直接决定系统能否稳定运行、能否被多人协作维护。本版方案就是针对这批"真实、可复现、有行号"的问题，给出**优先级 + 落地清单**。

核查结论一句话：核心功能已完整，但代码仍是**单机原型级**——存在静默吞错、资源泄漏、导出缺陷、四端代码大量复制、无密码认证等问题。

---

## 二、已确认缺陷清单（带代码证据）

以下每一条都经代码核查确认，附文件路径与行号。按严重度排序。

### 🔴 P0 — 功能性 Bug / 数据正确性

| # | 问题 | 证据位置 | 影响 |
|---|------|----------|------|
| 1 | **OPS 历史工单导出忽略筛选条件** | `frontend-ops/src/views/TicketHistory.vue:158` 硬编码 `opsApi.exportTickets(30)` | 页面设了 keyword/status/category 筛选，导出的却永远是"最近30天全部工单"，导出结果与用户所见不一致——数据类系统的严重可信度问题 |
| 2 | **导出失败被空 catch 吞掉** | 同文件 `:164` `catch (e) {}`；`frontend-ops/src/views/Overview.vue:168` | 导出失败（如 401、后端 500）用户完全无感知，以为在下载实则什么都没发生 |
| 3 | **admin 全局 WS 心跳定时器泄漏 + 无限重连** | `frontend/src/store/user.js:59-81` | `onopen` 里 `setInterval(...30000)` 从不 `clearInterval`；`onclose` 固定 5s 无上限无退避重连。每断开重连一次就多一个僵尸心跳定时器和一个旧 WS 引用，长时间运行内存与连接数持续累积 |

### 🟡 P1 — 健壮性 / 安全

| # | 问题 | 证据位置 | 影响 |
|---|------|----------|------|
| 4 | **无密码认证，登录即自动建号** | `backend/app/api/auth.py:30-59`（查不到用户直接 `db.add(User(role=USER))` 发 token）；4 端 `Login.vue` 无密码字段 | 任何人输入任意 ID 即可登录甚至凭空创建账号，无任何凭据校验——**上线前必须解决的安全红线** |
| 5 | **JWT 密钥为硬编码占位符** | `backend/app/config.py:49` `JWT_SECRET_KEY = "jwt-secret-key-change-in-production"` | 不配 `.env` 即用公开弱密钥，token 可被伪造 |
| 6 | **全局 500 处理器把内部错误原文返回客户端，且只 print 不落文件** | `backend/app/main.py:181-182` `print(tb)` + 响应体返回 `str(exc)` | 泄露堆栈/内部细节；日志只进 stdout（`main.py:27-30` 仅 `basicConfig`，无 FileHandler），重启即失，线上无法排查 |
| 7 | **调试 print 泄露用户信息** | `backend/app/api/auth.py:67,93` `print(f"DEBUG LOGIN...")` | 生产噪音 + 信息泄露 |
| 8 | **agent 端 WS 无退避/无上限/无心跳** | `frontend-agent/src/views/AgentChat.vue:190-229` 固定 3s 重连 | 后端抖动时高频重连风暴；死连接无法及时回收 |
| 9 | **401 清理不一致：3 端只删 token 不删 user** | `frontend-client/src/api/index.js:19`（agent/ops 同）；仅 `frontend/src/api/index.js:26-27` 删 user | token 过期后 user 对象残留 localStorage，刷新前 UI 仍显示"已登录"，状态错乱 |
| 10 | **Pydantic 请求模型几乎无 Field 约束** | 全后端无 `Field(`/`constr`/`ge/le`；如 `TicketRate.rating` 注释 1-5 但无 `ge/le`（`schemas/ticket.py:38`）；`chat.py:249` 非法 `msg_type` 抛未捕获 `ValueError` | 脏数据可入库；非法枚举值触发 500 |

### 🟢 P2 — 可维护性 / 性能

| # | 问题 | 证据位置 | 影响 |
|---|------|----------|------|
| 11 | **四端代码大量复制，无共享层** | `statusType/statusText/formatTime/slaColor/priorityType` 在 **9+ 个 .vue** 逐份复制（如 `TicketHistory.vue:167-171`、`Chat.vue:269-275`）；4 套 `api/index.js`、4 套 `store/user.js` 近乎逐字重复 | 改一处状态映射要改 9 处，极易漏改导致四端不一致 |
| 12 | **大量空 catch 静默吞错** | `frontend-agent/src/views/TicketDetail.vue` 7 处（`:239,282,290,298,321,329,337`）；`Dashboard.vue:138,167`；`Chat.vue:121-123` 仅 console.log | 加载失败对用户静默，白屏/无反馈难排查 |
| 13 | **数据库几乎无索引** | `backend/app/models/`：仅 `ticket_no`、`feishu_user_id` 有索引；`status/created_at/creator_id/assignee_id/sla_deadline`、`ChatMessage.room_id`、`TicketLog.ticket_id` 全无 index | 数据量上来后工单列表、看板、聊天记录查询全表扫描变慢 |
| 14 | **快捷回复模板纯内存存储** | `backend/app/api/templates.py:14-23` `_templates: list[dict]` | 重启丢失、多进程不共享，代码自注"生产应用数据库" |
| 15 | **无全局前端错误边界 / 无骨架屏** | 4 个 `main.js` 无 `app.config.errorHandler`；全项目无 `el-skeleton`，加载态全是 spinner | 组件运行时错误 → 整页白屏；感知加载偏慢 |
| 16 | **无移动端适配** | 全 `src/` **零 `@media`**；登录卡 `width:420px`、OPS `el-col :span` 固定栅格 | 平板/手机布局错乱溢出 |
| 17 | **无管理员操作审计** | `backend/app/api/admin.py` 改用户/改权限/审批/分类 CRUD 均无日志记录；无 `AuditLog` 模型 | 敏感操作无法追溯 |

---

## 三、分阶段实施方案

### 阶段一：紧急缺陷修复（0.5–1 天）—— 对应 P0 #1~#3

目标：消灭三个已确认、影响数据正确性/资源的 Bug。改动小、见效快、风险低，优先做。

**任务 1.1 — 修复导出忽略筛选（#1 #2）**
- `frontend-ops/src/views/TicketHistory.vue:156-165`：
  - 让 `handleExport` 把当前 `filters`（keyword/status/category_id）与日期范围传给 `opsApi.exportTickets(...)`，替换硬编码的 `30`。
  - 参照后端 `GET /api/ops/export`（`backend/app/api/ops.py`）实际支持的查询参数对齐字段名。
  - `catch (e) {}` → `ElMessage.error('导出失败：' + (e.message || '请稍后重试'))`；成功时 `ElMessage.success`。
  - 同样修 `Overview.vue:168` 的空 catch。

**任务 1.2 — 修复 admin WS 心跳泄漏与重连（#3）**
- `frontend/src/store/user.js:50-83`：
  - 把心跳 `setInterval` 的句柄存为变量，在 `onclose`/`logout` 里 `clearInterval`。
  - 重连改为指数退避 + 上限（对齐 `frontend-client/src/views/Chat.vue:95-169` 已有的 `MAX_RECONNECT=10` + `min(3000*2^n,30000)` 实现）。
  - `logout()`（`:92-102`）中确保清掉心跳定时器与重连计时器。

### 阶段二：安全与可观测性加固（2–3 天）—— 对应 P1 #4~#10

**任务 2.1 — 认证安全（#4 #5）**
- 决策点（需产品/负责人拍板，见"待确认")：是否引入密码/OTP，或仅先关闭"自动建号"并强制 `.env` 配置密钥。
- 最低限度：`auth.py:46-59` 移除"查不到用户即自动建号"逻辑（改为返回 401），账号创建收敛到 admin 用户管理；`config.py:49` 改为无默认值、启动时校验 `.env` 必须提供。

**任务 2.2 — 全局异常与日志（#6 #7）**
- `main.py:177-182`：500 响应体改为通用文案（不返回 `str(exc)`），内部用 `logger.error(exc, exc_info=True)` 记录。
- `main.py:27-30`：`logging.basicConfig` 增加 `RotatingFileHandler` 落 `backend/logs/app.log`（这也正好补上用户口中"查看日志文件"时应有的真实日志文件）。
- 删除 `auth.py:67,93` 的 DEBUG print（改用 `logger.debug`）。

**任务 2.3 — 前端拦截器与 WS 一致性（#8 #9）**
- 统一 4 端 axios 401 处理：都同时清 `token` + `user` + `permissions`（对齐 `frontend/src/api/index.js:26-27` 的完整版）。
- `frontend-agent/src/views/AgentChat.vue:190-229` WS 补退避+上限+心跳，复用阶段三抽出的公共 composable。

**任务 2.4 — 输入校验（#10）**
- 给关键 Pydantic 模型加 `Field` 约束：`TicketRate.rating: int = Field(ge=1, le=5)`；`TicketCreate.title` 加 `max_length`；`TemplateCreate` 长度约束等。
- `chat.py:249` 用 try/except 包裹 `MessageType(...)`，非法值返回 400 而非 500。

### 阶段三：工程质量与可维护性（3–5 天）—— 对应 P2 #11~#17

**任务 3.1 — 抽取前端公共层（#11 #12）** ★ 收益最高
- 新建共享包（`shared/` 目录 + npm workspace，或简单软链接/发布内部包），统一收纳：
  - `utils/format.js`：`statusType/statusText/formatTime/slaColor/priorityType`（消除 9+ 处复制）。
  - `api/request.js`：统一 axios 实例 + 拦截器。
  - `composables/useWebSocket.js`：以 client 端 `Chat.vue` 的实现为基准（退避+上限+心跳），供 4 端复用。
  - `stores/user.js`：统一登录/登出/token 管理。
- 顺带把空 catch 逐个补上用户可见反馈或 logger。

**任务 3.2 — 数据库索引（#13）**
- `backend/app/models/` 给高频查询字段加 `index=True`：`Ticket.status/created_at/creator_id/assignee_id/sla_deadline`、`ChatMessage.room_id/created_at`、`TicketLog.ticket_id`、`PermissionRequest.user_id/status`。
- 用 alembic（仓库已有 `backend/alembic/`）生成迁移。

**任务 3.3 — 模板持久化（#14）**
- 为 `templates.py` 建 `Template` 模型 + 表，CRUD 改为走 DB（参照现有 category 模型/CRUD 工厂写法）。

**任务 3.4 — 前端韧性（#15 #16）**
- 4 端 `main.js` 注册 `app.config.errorHandler`，并加一个全局错误边界组件避免整页白屏。
- 列表/详情页把 `v-loading` spinner 换成 `el-skeleton`。
- 关键页补 `@media` 断点（登录页、看板、OPS 图表）。

**任务 3.5 — 操作审计（#17）**
- 新增 `AuditLog` 模型；`admin.py` 的用户/权限/分类写操作统一记录（操作人、动作、目标、时间）。

---

## 四、优先级与工作量总览

| 阶段 | 内容 | 缺陷编号 | 工作量 | 风险 |
|------|------|----------|--------|------|
| 一 | 紧急缺陷修复 | #1 #2 #3 | 0.5–1 天 | 低 |
| 二 | 安全与可观测性 | #4~#10 | 2–3 天 | 中（认证改动需评估兼容） |
| 三 | 工程质量 | #11~#17 | 3–5 天 | 中（公共层抽取需回归四端） |

**建议顺序**：阶段一立即做（纯修 Bug，几乎无副作用）→ 阶段三的 3.1 公共层（后续所有前端改动都受益）→ 阶段二安全 → 阶段三剩余。

---

## 五、验收与验证方式

- **导出修复**：OPS 历史工单页设置 status=已解决 + 某分类 + 关键词，点导出，打开 xlsx 核对内容与筛选一致；断网/401 时应弹出错误提示。
- **WS 泄漏修复**：admin 端登录后反复触发断网重连（DevTools Network offline/online），检查只有一个心跳定时器（可在 `setInterval` 处打点计数），无 WS 引用堆积。
- **认证/日志**：不配 `.env` 时后端应拒绝启动或用告警密钥；触发一个 500，确认 `backend/logs/app.log` 有堆栈、客户端只收到通用文案。
- **索引**：`EXPLAIN QUERY PLAN` 检查工单列表查询是否命中索引；跑 `backend/tests/test_api.py`（当前 51/52）确保无回归。
- **公共层**：改动 `statusText` 一处映射，确认四端同步生效。
- **回归**：每阶段结束跑一遍 `cd backend && python tests/test_api.py`。

---

## 六、需产品/负责人确认的决策点

1. **认证方案（#4）**：走"密码登录"、"飞书 OAuth 真集成"，还是"暂时仅关闭自动建号 + 强制配置密钥"？影响阶段二工作量。
2. **前端共享层形态（3.1）**：采用 npm/pnpm workspace 改造成 monorepo，还是先建一个内部共享包/软链接？前者更规范但改动大。
3. 是否需要同步更新 `FEATURES.md` / `CLAUDE.md` 以反映本轮修复。
